<?xml version="1.0" encoding="utf-8"?>

<selector xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <!-- 按壓時 -->
    <!--
        <item android:state_pressed="true">
    -->
    <!--rectangle 長方形 可以畫出直角形，圓角形，弧形等-->
    <!--
        <shape android:shape="rectangle">
        -->
    <!--設定圓角，當設置的圓角半徑很大，就可變成弧形邊了-->
    <!--
                <corners android:radius="15dip" />
    -->
    <!--邊框-->
    <!--
                <stroke android:width="5dip" android:color="#F00078" />
    -->
    <!--漸層顏色-->
    <!--
                <gradient android:angle="-90" android:startColor="#FFC1E0" android:endColor="#FFD9EC"  />
            </shape>
        </item>
    -->
    <!-- 預設時 -->
    <item android:color="@android:color/black">
        <shape android:shape="rectangle"  >
            <corners android:radius="25dip" />
            <stroke android:width="0dip" android:color="#FFECF5" />
            <gradient android:angle="-90" android:startColor="#00000000" android:endColor="#00000000"  />
        </shape>
    </item>

</selector>
